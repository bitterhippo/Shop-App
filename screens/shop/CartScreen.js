import React from 'react';
import { View, Text, FlatList, StyleSheet, Button } from 'react-native';

const CartScreen = props => {
  return (
    <View>
      <Text>Total: {19.99}</Text>
    </View>
  )
};

const styles = StyleSheet.create({

});

export default CartScreen;